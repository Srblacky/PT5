<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bignerdranch.android.pt">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Main2Activity"
            android:label="Add A Customer" />
        <activity
            android:name=".BillInfoPayment"
            android:label="@string/title_activity_bill_info_payment" />
        <activity
            android:name=".Camera"
            android:label="@string/title_activity_camera" />
        <activity
            android:name=".DisplayCustomerListPics"
            android:label="@string/title_activity_display_customer_list_pics" />
        <activity
            android:name=".ReceiptInfo"
            android:label="@string/title_activity_receipt_info" />
        <activity
            android:name=".ViewCustSessionCompleteAddSign"
            android:label="@string/title_activity_view_cust_session_complete_add_sign"></activity>
    </application>

</manifest>